                       https://www.youtube.com/watch?v=G3e-cpL7ofc    
				(Tutorial HTML / CSS)
					  HTML
Whats is HTML?/Oque é HTML?
HTML = Giving instrucions to a computer / HTML = Envia instruções a um cumputador
					
					  HTML Element

HTML Element = An HTML element is defined by a start tag, some content, and an end tag. 
/ Um elemento HTML é definido por uma tag inicial, algum conteúdo e uma tag final.
					
					  Elements 

<button> Createa a button </button> - Instruction to creat a Button / Instrução para criar um botão.
<p> Paragraph of text  </p> - Instruction to creat a Paragraph / Instrução para criação de um botão.
<a> Anchor Element </a> - Instruction to creat a Anchor / Instrução para criar uma ancora (
                           (Link to another website)    /     (Link para outro site))
<a href="https://github.com/DevMarceloFlores"> Link GitHub </a>
<strong> deixa oque esta dentro da tag em negrito </strong>
<u> Sublinha oque está dentro da tag</u>
<span> Permite que eu altere só oque está dentro do span </span>
					  Syntax

Syntax = rules for writing code / São as regras de como uma linguagem de codificação como o HTML deve ser escrita.
       = grammar(for code) / A gramatica(do codigo)

("If you think about a language like English, we have something called grammar, which is the rules of the language. 
 The syntax is basically the same thing, except for the coding languages. Now, the biggest difference between a language like English and a coding language is that in English you don't have to follow the grammatical rules perfectly.")

(" Se pensar numa língua como o português, temos algo chamado gramática, que ~soa as regras da língua. 
 A sintaxe é basicamente a mesma coisa, excepto para as linguagens de codificação. Ora, a maior diferença entre uma língua como o português e uma línguagem de codificação é que em português não tem que seguir perfeitamente as regras gramaticais.")

					Rules Syntax

     (Each element consists of two labels: one for opening and one for closing. 
                                             /
      Cada elemento consite em duas etiquetas: uma de abertura de outra de fechamento)
  
       So what are the syntax rules of HTML? / Então quais são as regras sintaxe do html?
             * HTML Tag
	     * Tag Name
					HTML Attribute

             Modifies how an element behaves / Modifica o comportamento de um elemento.

Href = It determines where this link element will take us
target = Determines whether this link opens on the current page or in a new tab.
(only works on the link element)
  - _blanck - Opens a new page and leaves the current one open.
(So, by default, if we don't have a target attribute, it opens on the current page.)

Href = Ele determina para aonde este elemento de ligação nos levará
target = Determina se esse link abre na página atual ou num novo separador.
(só funciona no elemento link)
  -	_blanck - Abre uma nova pagina e deixará a tual aberta.
( Assim, por defeito, se nã tivermos um atributo de destino, abre na página atual.)

Class="" - da o nome a um elemento